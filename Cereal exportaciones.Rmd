---
title: "Tourism"
author: "Mauro Rodriguez"
date: "13 de 2021"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

#Introducir la serie de tiempo en notaci?n cient?fica o formato general
```{r}
setwd("C:/Users/L01191825/Documents/CADI Analitica_R")

exp_cereal<-read.csv("Cereal_Ventas.csv")

exp_cereal
```


```{r}
#library(tseries)
```

```{r}
ts.exp_cereal=ts(exp_cereal, frequency =12, start=c(1993,1), end = c(2020,11))

```



```{r}

plot(ts.exp_cereal)

```
```{r}
ts.exp_cereal
```


```{r}
dim(as.matrix(ts.exp_cereal))
```
#Base de datos para entrenamiento y prueba

```{r}
exp_cereal.train=window(ts.Miles_de_Turistas, start=c(2000,1), end=c(2016,12))
plot(exp_cereal.train)

```

```{r}
exp_cereal.train
```


```{r}
dim(as.matrix(exp_cereal.train))
```

```{r}
exp_cereal.test=window(ts.exp_cereal, start=c(2017,1), end=c(2019,4))
plot(exp_cereal.test)
```

```{r}
library(forecast)

arima1=auto.arima(exp_cereal.train, trace=TRUE, test = "kpss",ic="bic" )
```

```{r}
summary(arima1)
confint(arima1)
```

#Diagn?stico de residuales

```{r}
install.packages("tseries")
install.packages("forecast")
install.packages("TSPred")
library(forecast)
library(tseries)
library(TSPred)
```

#En lugar de la funci?n forecast.Arima use la funci?n "forecast"
```{r}
arima1.forecast=forecast(arima1, h=36)

arima1.forecast
```


```{r}
plot(arima1.forecast, xlab="Year", ylab="Exportaciones de cereal (miles))")
```

```{r}
library(TSPred)
```


```{r}

plotarimapred(exp_cereal.test,arima1,xlim=c(2016,2018), range.percent = 0.05, ylab = "Exportaciones de cereal",
xlab = "---- real  ____ proyección")

```

```{r}
accuracy(arima1.forecast,exp_cereal.test)

```


```{r}
arima1.forecast=forecast(arima1, h=60)

arima1.forecast
```

```{r}
plot(arima1.forecast)
```

